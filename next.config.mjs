// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Uncomment if you're using the src directory for pages
  // pageExtensions: ['js', 'jsx', 'ts', 'tsx'],
};

export default nextConfig;
